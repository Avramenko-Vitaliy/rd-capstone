apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: rd-app
  namespace: flux-system
spec:
  interval: 5m
  values:
    rdApp:
      api:
        enabled: false
        replicas: 1
        image:
          tag: dev # {"$imagepolicy": "flux-system:rd-api:tag"}
      ui:
        enabled: false
        replicas: 1
        image:
          tag: dev # {"$imagepolicy": "flux-system:rd-ui:tag"}
