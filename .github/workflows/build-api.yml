name: Build API

on:
  push:
    branches: [ "dev", "prod" ]
    paths:
      - 'rd-api/**'
  pull_request:
    branches: [ "dev", "prod" ]
    paths:
      - 'rd-api/**'

jobs:
  ci:
    uses: ./.github/workflows/ci-template.yml
    with:
      module: rd-api
    secrets: inherit
