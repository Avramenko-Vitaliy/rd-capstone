apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: todo-api
  namespace: flux-system
spec:
  filterTags:
    pattern: '^prod-(?P<timestamp>.*)$'
    extract: '$timestamp'
  policy:
    alphabetical:
      order: asc
