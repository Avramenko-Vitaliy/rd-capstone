apiVersion: image.toolkit.fluxcd.io/v1
kind: ImageRepository
metadata:
  name: todo-ui
  namespace: flux-system
spec:
  image: vitaliyavramenko/todo-ui
  interval: 5m
---
apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: todo-ui
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: todo-ui
  filterTags:
    pattern: '^latest$'
  policy:
    alphabetical: {}
  digestReflectionPolicy: Always
  interval: 10m
