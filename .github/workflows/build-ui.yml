name: Build UI

on:
  push:
    branches: [ "master" ]
    paths:
      - 'rd-ui/**'
  pull_request:
    branches: [ "master" ]
    paths:
      - 'rd-ui/**'

jobs:
  ci:
    uses: ./.github/workflows/ci-template.yml
    with:
      module: rd-ui
    secrets: inherit
