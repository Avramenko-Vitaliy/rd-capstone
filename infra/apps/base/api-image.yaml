apiVersion: image.toolkit.fluxcd.io/v1
kind: ImageRepository
metadata:
  name: todo-api
  namespace: flux-system
spec:
  image: vitaliyavramenko/todo-api
  interval: 5m
---
apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: todo-api
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: todo-api
  filterTags:
    pattern: '^latest$'
  policy:
    alphabetical: {}
  digestReflectionPolicy: Always
  interval: 10m
