apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: rd-ui
spec:
  filterTags:
    pattern: '^prod-(?P<timestamp>.*)$'
    extract: '$timestamp'
    policy:
      alphabetical:
        order: asc
---
apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: rd-api
spec:
  filterTags:
    pattern: '^prod-(?P<timestamp>.*)$'
    extract: '$timestamp'
    policy:
      alphabetical:
        order: asc
