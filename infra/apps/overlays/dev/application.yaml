apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: rd-app
  namespace: flux-system
spec:
  interval: 5m
  values:
    rdApp:
      api:
        replicas: 1
        image:
          tag: dev-20251219-115145 # {"$imagepolicy": "dev:rd-api:tag"}
      ui:
        replicas: 1
        image:
          tag: dev-20251217-144137 # {"$imagepolicy": "dev:rd-ui:tag"}
