apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: rd-app
  namespace: flux-system
spec:
  interval: 5m
  values:
    rdApp:
      api:
        replicas: 1
        image:
          tag: dev-20251217-153643 # {"$imagepolicy": "flux-system:rd-api-dev:tag"}
      ui:
        replicas: 1
        image:
          tag: dev-20251217-144137 # {"$imagepolicy": "flux-system:rd-ui-dev:tag"}
