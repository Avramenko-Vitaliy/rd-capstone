name: Build API

on:
  push:
    branches: [ "master" ]
    paths:
      - 'rd-api/**'
  pull_request:
    branches: [ "master" ]
    paths:
      - 'rd-api/**'

jobs:
  ci:
    uses: ./.github/workflows/ci-template.yml
    with:
      module: rd-api
    secrets: inherit
