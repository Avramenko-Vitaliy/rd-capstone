name: Build UI

on:
  push:
    branches: [ "dev", "prod" ]
    paths:
      - 'rd-ui/**'
  pull_request:
    branches: [ "dev", "prod" ]
    paths:
      - 'rd-ui/**'

jobs:
  ci:
    uses: ./.github/workflows/ci-template.yml
    with:
      module: rd-ui
    secrets: inherit
