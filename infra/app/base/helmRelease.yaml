apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: rd-app
  namespace: flux-system
spec:
  interval: 5m
  chart:
    spec:
      chart: ./infra/charts/rd-app
      version: "v1.3.1"
      sourceRef:
        kind: GitRepository
        name: rd-capstone
        namespace: flux-system
